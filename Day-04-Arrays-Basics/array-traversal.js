//// Adding loops from Day 3 to arrays.
const techstack = ["JavaScript", "Node.js", "C#", "Entity Framework"];

for (let i = 0; i < techstack.length; i++){
    console.log(`Tech ${i + 1}; ${techstack[i]} `);
}
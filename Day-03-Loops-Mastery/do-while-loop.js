// This loop must run at least once, even if the condition is already false.

console.log("--- DO-WHILE LOOP ---");

let i = 20;
do{
    console.log("Do-While runs at least once:" , i);
    i++;
}while(i <5); //this is false but it will run once 